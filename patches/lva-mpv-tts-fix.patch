diff --git a/linux_voice_assistant/mpv_player.py b/linux_voice_assistant/mpv_player.py
index 8dcc15c..b6c9953 100644
--- a/linux_voice_assistant/mpv_player.py
+++ b/linux_voice_assistant/mpv_player.py
@@ -46,12 +46,11 @@ class MpvMediaPlayer:
             self._log.warning("play() called with empty URL list")
             return
 
-        # Track is changing - stop if needed
-        if self._done_callback is not None:
-            if self._player.state() != PlayerState.IDLE:
-                self._log.debug("Stopping active playback before starting new media")
-                self._player.stop(for_replacement=True)
-            self._done_callback = None
+        # Stop any active playback before starting new media
+        if self._player.state() != PlayerState.IDLE:
+            self._log.debug("Stopping active playback before starting new media")
+            self._player.stop(for_replacement=True)
+        self._done_callback = None
 
         self._log.info("Playing %d URL(s): %s", len(urls), urls[0])
         
